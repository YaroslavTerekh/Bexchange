import { AdminBookTextTrimPipe } from './admin-book-text-trim.pipe';

describe('AdminBookTextTrimPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminBookTextTrimPipe();
    expect(pipe).toBeTruthy();
  });
});
